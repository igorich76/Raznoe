{$reference 'System.IO.Compression.FileSystem.dll'}
program pr_zip;
uses System;
uses System.IO.Compression;
begin
 var (a,b):=ReadlnString2('Введите путь к папке и имя будущего архива(его полный путь + имя.rar)');
 system.io.compression.ZipFile.CreateFromDirectory(a, b);
 
end.
