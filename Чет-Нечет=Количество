##
var (a,b) := (0,0);
for var i := 1 to 200 do begin  
  var c := Random(1,100);
  if (c mod 2 = 0) then a += 1
    else b += 1;
end;{для for}
Println('Четных чисел=',a);
Println('Нечетных чисел=',b);
Print('Итого=',a + b);
// сам себе придумал задачу
